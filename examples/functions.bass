; Using `call` and `return` for basic function calling

func2:
  println "inside func2"
  return

func:
  println "inside func"
  call func2
  return

_:
  println "before"
  call func
  println "after"
